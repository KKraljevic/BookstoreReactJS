/*
 * CategoryBooksPage Messages
 *
 * This contains all the text for the CategoryBooksPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CategoryBooksPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CategoryBooksPage container!',
  },
});
