// import { selectCategoryBooksPageDomain } from '../selectors';

describe('selectCategoryBooksPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
